namespace Amazon
{
    // you can access this class from the outside if you're still within the same assembly. But e.g
    // AccessModifiers project cannot access it because this is in the Amazon project
    internal class RateCalculator
    {
        public int Calculate()
        {
            return 0;
        }
    }
}
